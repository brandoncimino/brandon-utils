<!-- Is this file actually respected at all, or is only the one at the project-level used? -->
<Project>
    <PropertyGroup>
        <TargetFramework>netstandard2.1</TargetFramework>
        <Nullable>enable</Nullable>
    </PropertyGroup>
</Project>